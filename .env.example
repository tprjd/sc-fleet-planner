# Host of the Cloudflare Worker (no protocol).
# Local dev: run `npm run worker:dev` and use localhost:8787.
# Production: your deployed worker, e.g. sc-fleet-planner.<subdomain>.workers.dev
VITE_WORKER_HOST=localhost:8787
